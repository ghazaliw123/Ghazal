# راهنمای اجرای پروژۀ شبیه‌سازی حملۀ DDoS

## 📋 نیازمندی‌ها

- Python 3.6 یا بالاتر
- Windows / Linux / macOS

## 🚀 نحوه نصب و اجرا

### مرحلۀ 1: نصب کتابخانه‌های مورد نیاز

```bash
pip install -r requirements.txt
```

### مرحلۀ 2: اجرای پروژه

#### گزینۀ 1: اجرای خودکار (توصیه شده)

```bash
python run_project.py
```

این دستور:
- سرور را شروع می‌کند
- 30 ثانیه برای نمونه‌گیری حالت عادی صبر می‌کند
- حملۀ DDoS را شروع می‌کند
- نمودارها را رسم می‌کند

#### گزینۀ 2: اجرای دستی

**Terminal 1 - سرور:**
```bash
python server.py
```

**Terminal 2 - حمله (بعد از 30 ثانیه):**
```bash
python ddos_attack.py
```

**Terminal 3 - نمودار (بعد از تمام شدن حمله):**
```bash
python plot_metrics.py
```

## 📊 نتایج

بعد از اجرا، دو فایل تولید می‌شوند:

1. **metrics.json** - داده‌های خام جمع‌آوری شده
   - CPU usage
   - Memory usage
   - زمان‌ها
   - تعداد درخواست‌ها

2. **ddos_analysis.png** - نمودارهای تصویری
   - نمودار CPU
   - نمودار حافظه

## ⚙️ تنظیمات

اگر می‌خواهید پارامترهای حمله را تغییر دهید، فایل `ddos_attack.py` را ویرایش کنید:

```python
attack = DDoSWithProxies(
    target_ip='localhost',      # IP هدف
    target_port=8000,           # پورت هدف
    num_threads=50              # تعداد نخ‌ها (بیشتر = حمله قوی‌تر)
)
attack.start_ddos(duration=30)  # مدت حمله (ثانیه)
```

## 🛠️ حل مشکلات

### مشکل 1: خطای "Port already in use"

**حل:**
سرور قبلی هنوز در حال اجرا است. پورت 8000 را ببندید:

```bash
# Windows
netstat -ano | findstr :8000
taskkill /PID <PID> /F

# Linux/Mac
lsof -i :8000
kill -9 <PID>
```

### مشکل 2: خطای "ModuleNotFoundError: No module named 'psutil'"

**حل:**
```bash
pip install psutil
```

### مشکل 3: نمودار نمایش داده نمی‌شود

**حل:**
مطمئن شوید که `metrics.json` ایجاد شده است:

```bash
python plot_metrics.py
```

## ⚠️ ملاحظات امنیتی

⚠️ **این پروژه فقط برای مقاصد آموزشی و تحقیقی است!**

**قوانین استفاده:**
- ❌ هرگز روی سرورهای دیگران استفاده نکنید
- ❌ هرگز روی شبکه‌های عمومی استفاده نکنید
- ✅ فقط روی محیط‌های شخصی و ایزوله
- ✅ فقط برای یادگیری و تحقیق

**مسئولیت قانونی:**
حمله DDoS روی سرورهای واقعی **جرم است** و می‌تواند منجر به:
- تحقیقات پلیسی
- جریمۀ نقدی سنگین
- زندان

## 📚 فایل‌های پروژه

| فایل | توضیح |
|------|--------|
| `server.py` | سرور HTTP و نظارت بر منابع |
| `ddos_attack.py` | برنامۀ حملۀ DDoS |
| `plot_metrics.py` | رسم نمودارهای CPU و حافظه |
| `run_project.py` | برنامۀ اجرای اتوماتیک |
| `requirements.txt` | لیست کتابخانه‌های مورد نیاز |
| `INSTRUCTIONS.md` | این فایل |
| `.gitignore` | فایل‌های نادیده گرفتن شده در Git |

## 📞 سوال یا مشکل؟

اگر سوالی دارید، لطفاً مسئله را باز کنید یا نظر بگذارید.

---

**تاریخ ایجاد:** 8 خرداد 1405
